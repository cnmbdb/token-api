**Source visual truth**

- Desktop capture: `.design-reference/source-desktop-00.png`
- Mobile capture: `.design-reference/source-mobile-top-00.png`
- Interaction captures: `.design-reference/source-desktop-protocol-open.png`, `.design-reference/source-desktop-quickstart-codex.png`, `.design-reference/source-desktop-routing-cost.png`, `.design-reference/source-desktop-live-anthropic.png`, `.design-reference/source-desktop-faq-open.png`, `.design-reference/source-mobile-menu-open.png`

**Implementation evidence**

- Desktop detail pass: `.design-reference/implementation-desktop-detail-normalized.png`
- Mobile detail pass: `.design-reference/implementation-mobile-detail-normalized.png`
- Mobile menu: `.design-reference/implementation-mobile-detail-menu.png`
- GSAP title frames: `.design-reference/implementation-title-gsap-frame-1.png`, `.design-reference/implementation-title-gsap-frame-2.png`
- Mobile GSAP title: `.design-reference/implementation-mobile-gsap-title-normalized.png`
- Combined comparisons: `.design-reference/qa-desktop-detail-side-by-side.png`, `.design-reference/qa-mobile-detail-side-by-side.png`
- Focused title comparison: `.design-reference/qa-mobile-gsap-title-side-by-side.png`
- Logo source/implementation comparison: `.design-reference/qa-logo-source-implementation-side-by-side.jpg`
- Logo page captures: `.design-reference/implementation-logo-suxin-top.png`, `.design-reference/implementation-logo-suxin-footer.png`

**Viewport and normalization**

- Desktop source: 1440 x 900 CSS px, DPR 1; source capture 1440 x 900 px.
- Desktop implementation: verified at 1440 x 900 CSS px; the in-app stage capture was normalized back to 1440 x 900 for the combined comparison.
- Mobile source: 390 x 844 CSS px, DPR 1; source capture 390 x 844 px.
- Mobile implementation: verified by browser read-back at 390 x 844 CSS px with no horizontal overflow. The in-app browser returned a 433 x 938 stage capture with the 390 x 844 page scaled inside it; the page region was cropped and normalized to 390 x 844 before comparison.
- State: Chinese landing page, page top, OpenAI protocol selected, mobile navigation closed.

**Full-view comparison evidence**

- Desktop: 76 px navigation, 900 px hero, pill at approximately y=120, title at approximately y=169, centered CTA/endpoint controls, model/client nodes, animated route paths, orange/blue hero field, and content width follow the source. The suxin brand and new-api destinations are intentional product substitutions.
- Mobile: fixed 58 px navigation, 390 px overflow-free content, hero pill/title/actions/endpoint order, animated title wrapping, compact route diagram, warm gradient crop, and six-item mobile menu follow the source.

**Focused region comparison evidence**

- Hero controls were checked at matched mobile CSS dimensions. Implementation read-back: pill y=316 px, title y=383 px, actions y=498 px, endpoint y=574 px.
- The revised GSAP title was checked at 390 x 844 CSS px: 39.975 px font size, 43.173 px line height, -0.48 px letter spacing, fixed 92 px title box, exactly two lines per frame, and zero horizontal overflow. The CTA and endpoint remain fixed at y=418 and y=477 while the title changes.
- Pricing cards, quick-start code area, routing tabs, discount filters/chart, ranking cards, FAQ accordion, footer, and final image banner were inspected in the browser during the long-page pass.
- Source assets are local files; no target-site asset is hotlinked.

**Required fidelity surfaces**

- Fonts and typography: Space Grotesk source font is bundled locally; PingFang/Microsoft YaHei/system fallbacks preserve CJK rendering. Display scale, heavy weight, tight leading, and mono data labels match the source hierarchy.
- Spacing and layout rhythm: desktop three-column card grids and mobile single-column stacks preserve the source's square cards, dashed dividers, wide section spacing, and fixed navigation. Browser read-back confirmed no mobile horizontal overflow.
- Colors and visual tokens: warm `#faf9f4` base, near-black ink, orange accent, blue/orange hero gradient, subtle borders, and restrained surfaces map to the source.
- Image quality and asset fidelity: the captured hero, payment, and closing artwork plus source font remain local at original quality. The supplied suxin logo is now isolated as optimized black and white transparent PNG assets.
- Logo replacement: the user-supplied 1254 x 1254 transparent PNG was alpha-cropped without redrawing, padded to a square, and downsampled to a sharp 512 x 512 transparent asset. A white-alpha-preserving variant is used on the dark footer background.
- Copy and content: visual structure follows the source while TeamoRouter copy, URLs, API key naming, and destinations are replaced with suxin/new-api equivalents.
- Icons: source model/client icons, key image, gift badge, overlapping trust avatars, Bolt mark, logo mark, and source-equivalent navigation icons are used locally. No target-site asset is hotlinked.

**Interaction and runtime checks**

- Tested: mobile menu open/close; exact top-navigation labels and href read-back; protocol dropdown and Base URL update; GSAP 3.15 title timeline with both title frames; reduced-motion branch; animated route canvas and seven route nodes; quick-start method/language tabs; code copy handler; routing tabs; provider filtering; model search/list selection; discount chart update; and five FAQ accordions.
- Scroll reveal pass: locally bundled ScrollTrigger 3.15 registers 12 major section timelines. Browser read-back verified every section reached `entered`/`entered-back`, off-screen sections reached `left`/`reset`, and pricing replayed to full opacity with an identity transform after reverse scrolling.
- Motion performance: section timelines animate only opacity and transforms, stagger small target groups, release `will-change` after completion, refresh after fonts/load, and provide a no-motion path for `prefers-reduced-motion`.
- Browser console errors/warnings: none.
- Static validation: `git diff --check` passed.

**Findings**

- No actionable P0, P1, or P2 mismatch remains.

**Follow-up polish**

- No remaining animation or logo follow-up is required for the current scope.

**Comparison history**

- Pass 1 found mobile quick-start content expanding the document width from 433 to 506 CSS px. Added explicit mobile overflow containment and `min-width: 0` for quick-start children.
- Pass 2 browser read-back confirmed document width equals viewport width at the mobile breakpoint, with all primary interactions working and no console errors.
- Detail pass 3 restored the source navigation labels and mapped them to working suxin/new-api destinations; replaced the simplified hero chips with source icons and an animated canvas route diagram; added trust avatars, the Bolt mark, the gift badge, and source-level copy.
- Detail pass 4 compared source and implementation at 1440 x 900 and 390 x 844. Browser read-back confirmed zero horizontal overflow, seven loaded route nodes, a valid OpenClaw asset, title-frame rotation, protocol switching, and routing-tab state updates.
- Title pass 5 found the selected hero heading too wide, with overly tight tracking and a basic timer-based transition. Replaced it with a locally bundled GSAP 3.15 timeline, reduced desktop tracking from roughly -2 px to -0.6 px, constrained desktop width to 405-444 px, and fixed the title box height so downstream controls do not move.
- Title pass 6 verified both exact source title strings appear during a 5.6-second observation window. Focused mobile comparison confirmed two-line wrapping for both frames, stable CTA/endpoint positions, and zero overflow; no actionable P0/P1/P2 title finding remains.
- Logo pass 7 replaced the previous mark in the navigation, routing hub, footer, and favicon. Browser read-back confirmed all three visible logo instances load at 512 x 512 natural resolution, the navigation renders at 25 x 25, the hub at 31 x 31, the footer at 27 x 27, and no image is broken.
- Motion pass 8 added a dedicated ScrollTrigger timeline to all 12 post-hero sections. Desktop browser traversal verified title-first/card-second rhythm, enter/leave/back-scroll replay behavior, no horizontal overflow, and a local ScrollTrigger 3.15 runtime. The existing hero title timeline remains independent so its two-frame loop is not restarted by page scrolling.
- Navigation pass 9 removed only the visible `suxin` wordmark beside the top-left logo. Browser read-back confirmed the navigation brand contains no text span, the logo remains loaded and linked to the page top, the footer wordmark is unchanged, and there is no horizontal overflow.
- GitHub Pages pass 10 added an explicit document-relative base so the same static build resolves assets correctly both at `/token-api/` and at a future custom-domain root. A parent-directory server simulation loaded the page at `/token-api/` with GSAP 3.15, ScrollTrigger 3.15, all 12 reveal groups, all images, fonts, scripts, and artwork present; no broken image or horizontal overflow was detected.
- Internationalization pass 11 added IP-country language selection plus a real six-language menu for Simplified Chinese, Traditional Chinese, English, Japanese, Russian, and Hindi. Browser checks switched through all six complete page translations with zero overflow, verified English/Russian/Hindi contain no residual Han text outside the intentionally untranslated code sample, and confirmed protocol selection, provider filtering, route tabs, active integration instructions, title animation, and persistent manual selection remain functional. A fresh-origin Pages-path test resolved the current public IP country as `US`, selected English from the IP mapping, loaded every local asset, and kept all 12 ScrollTrigger groups active.
- CTA pass 12 measured the source hero controls in default and hovered states. Both source buttons are 52 px high; the primary uses a 48 x 46 px key knob, a 0.34 s cubic-bezier slide from left to right, a matching left-origin white fill, swapped horizontal padding, and a 19 px/500 label. The implementation now reproduces those values, adds the source-equivalent subtle hover fill to the client button, keeps both controls exactly 52 px high, preserves keyboard focus and reduced-motion behavior, and keeps all six translated labels inside the animated wrappers.
- Experience pass 13 rebuilt the four annotated source surfaces. Quick Start now has five real panels, eight API language samples, three CLI clients, two operating-system modes, client artwork, and three integration cards; browser clicks confirmed every primary tab updates the visible panel. The metrics cards use the three source WEBP textures and source icon assets, with GSAP hover read-back moving icon opacity from `0` to `.95` and clearing blur/rotation. The route decision graph reproduces the dotted canvas, app skeleton, forked solid/dashed edges, animated streak, selected/dimmed provider cards, and availability/cost/latency states; browser checks returned the exact questions, models, and result values for all three states. The finale now uses an animated high-DPI canvas tunnel, dither transition, source-scale label, and centered closing line; screenshots taken 500 ms apart differed. English, Russian, Hindi, and Simplified Chinese were rechecked, Hindi-specific route copy was corrected, all tested languages retained zero horizontal overflow, and Simplified Chinese was restored. No new browser console errors were emitted; Java/Rust/Ruby code tabs, `node --check`, and `git diff --check` passed.
- Logo-background pass 14 used the two annotated browser captures as the scoped source target and the supplied `assets/teamo-style/logo-suxin.png` / white variant as the required replacement asset. The original desktop and mobile halftone backgrounds were opened and inspected, then clean no-emblem versions were generated at `assets/teamo-style/buy-art-bg-clean.png` and `assets/teamo-style/buy-art-mobile-clean.png`. Browser-rendered evidence at 1241 × 845 confirmed the purchase section contains no mushroom silhouette, keeps the original blue/peach dither field, and displays the exact supplied black logo behind the heading. The animated finale keeps its tunnel and dither transition while the exact supplied white logo now replaces the prior centered emblem behind `SUXIN ROUTER`. Both logo assets loaded at 512 px natural width, the page retained zero horizontal overflow, and the browser console was empty. No P0/P1/P2 mismatch remains for the two annotated regions.
- Buy-hook pass 15 rebuilt the annotated `按量付费` section against the source DOM and measured CSS. Desktop read-back at 1117 px confirmed the source-matched 380 px section height, 40.212 px title with 45.842 px leading, animated blue/cyan/pink title fill, 52 px equal-height actions, 48 px primary knob, and four-step GSAP reveal with 90 ms staggering. The primary hover was verified from dark/white text with a left knob and zero-width fill to dark text with a full white fill and the knob at the right edge; the ghost button retains the source 6% ink hover wash. The six-language updater changes only `.pay-label`, so the fill and knob wrappers survive language switching. Mobile read-back at 390 × 844 confirmed the source 320 px compact height, hidden motion knob, translated labels, one active ScrollTrigger, zero horizontal overflow, and no failed requests or browser/page errors. `node --check` and `git diff --check` passed.
- Hero endpoint pass 16 measured the annotated source `#heroBaseurl` at 1117 × 761: 415.55 × 40 px, a 21.30 px CTA gap, 123.92 px protocol segment, 253.63 px URL segment, and a centered 32 px copy control. The local component now matches those desktop dimensions with a responsive 18–26 px gap, 68% white backdrop, 10% ink border, centered URL content, source dropdown chevron, source copy/check assets, orange path emphasis, and a true centered 32 px copy slot. Browser interaction checks opened and closed the protocol menu, switched to Anthropic, restored OpenAI, retained two copy-state icons through English and Simplified Chinese, preserved `https://suxin.ai/v1`, and reported zero overflow, broken images, warnings, or errors. The compact breakpoint hides only the URL scheme and keeps all three segments aligned.
- Hero background pass 17 reproduced the source stage layering from its measured `.art-bg::before`: solid `#fffefb` through 14%, a translucent white fade through 34%, and a fully revealed artwork field by 52%, with the blue/orange image anchored at `50% 78%`. The local Hero reads back at 900.000 px versus the source stage at 901.008 px. Desktop and compact browser captures confirm that the title and controls sit on the white upper field while the route diagram occupies the colored lower field; GSAP title/route behavior remains intact, document overflow is zero, every image loads, and the browser reports no warnings or errors.
- Ranking pass 18 rebuilt the annotated `使用排行榜` section from the source desktop and 390 × 844 mobile states. Source measurements showed a 1027.64 × 350.45 px usage panel at 1117 px, a 427.70 px mobile usage panel, 30 stacked daily columns, four Y-axis labels, and two five-row leaderboards. The local implementation reproduces the 92% desktop board, 350 px usage panel, 428 px compact panel, dashed perimeter/dividers, 30 seven-segment columns, three compact date labels, five Agent rows, five model rows, source OpenAI/Claude/VSCode/OpenClaw icons, and the daily update footer. Fake data is deterministic by calendar date: the current 30-day series follows a rising baseline from roughly 270B to 1.0T while adding date-seeded daily variation, includes nine visible pullbacks and twenty rising days, ends on 08/25, advances its baseline by 24.586B per new day, recalculates the day-over-day percentage from the final two displayed days, and scales both leaderboards from the same day index. Keyboard/mouse focus opens a detailed per-day tooltip; English and Simplified Chinese switching updates headings, descriptions, row metadata, and timestamp without rebuilding the controls. Browser read-back confirmed zero horizontal overflow, 30 bars, 10 rank rows, no broken assets, and no warnings or errors.
- Finale pass 19 replaced the earlier two-dimensional tunnel approximation with the source-equivalent native WebGL ray-marching scene. Source inspection confirmed a fixed, bottom-anchored finale revealed behind the opaque page/footer curtain, a 52vh desktop height and 34vh compact height, a 50-step three-domain shader, and the exact `dither-fade.svg` transition mask. The local implementation reproduces the fixed reveal, source shader parameters, focal point, travelling multicolour streaks, exposure, desktop/mobile typography, label treatment, and local dither asset while preserving the requested SUXIN branding and supplied-logo watermark. Browser read-back confirmed the `webgl-raymarching` renderer produced changing frames, stopped advancing while off-screen, rendered a still frame for reduced motion, used a 439.69 px band at 1241 × 845, normalized to the source's 395.72 px at 1117 × 761, and normalized to 286.95 px at 390 × 844. English and Simplified Chinese titles were verified, with zero horizontal overflow, broken images, warnings, or errors. Side-by-side source/local captures show the same centered vanishing point, light-trail density, footer-curtain reveal, dither boundary, label scale, and closing-line placement.
- Navigation-menu pass 20 captured the annotated source trigger in closed and open states. The implementation now keeps the exact 40 × 40 three-line control visible at desktop and compact widths, swaps it for the 23 px close mark, and opens the source-style fixed bottom sheet over a full-viewport 42% ink scrim with 2 px backdrop blur. Browser measurements match the source's 435.7 px sheet, 102.25 px header, 38 × 4 px grip, 47.25 px primary action, six 49.25 px navigation rows, orange active state, 340 ms spring-like sheet transition, and 260 ms scrim transition. The existing GitHub Pages-safe destinations are preserved; scrim click, menu-item click, and Escape all close the sheet while synchronizing `aria-expanded` and `aria-hidden`. Simplified Chinese menu copy was verified, compact read-back normalized to 390 × 844 with zero horizontal overflow, and the source/local open-state screenshots show matching trigger, dimmed page, sheet rhythm, CTA, active row, and menu order.
- Navigation-source pass 21 synchronized the sheet with the active `/Users/a2333/IDE/api/new-api` top-navigation hook and the live `https://suxintoken.com/api/status` configuration. The seven rows are now 主页、控制台、模型广场、排行榜、文档、关于、邀请返现; the first six use the exact enabled new-api routes, the live external documentation destination is `https://docs.newapi.pro`, and the retained referral entry targets the wallet page that contains the referral rewards card. The desktop and sheet “开始使用” controls now target `https://suxintoken.com/sign-in`. All eight destinations returned HTTP 200, all six language dictionaries contain seven aligned labels, the invitation gift asset remains attached to the final row, and both navigation scripts pass `node --check` plus `git diff --check`.
- CTA destination pass 22 scopes the new domain rule to button-like navigation only. Every rendered `a.btn`, pricing-card CTA, quick-start option, integration card, and mobile-sheet primary action now resolves to the canonical authentication entry point at `https://suxintoken.com/sign-in`. Menu rows keep their previously verified deep links, while language, protocol, copy, tabs, filters, routing, search, and FAQ buttons remain real in-page controls instead of being converted into navigation.
- Navigation chat pass 23 replaces only the former About row with a localized Group Chat entry targeting `https://token.macosabc.com/chat`. The other six menu destinations, invitation gift asset, CTA authentication routing, and all in-page controls remain unchanged.

final result: passed
